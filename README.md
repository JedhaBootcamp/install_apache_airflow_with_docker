# Install Apache Airflow locally with Docker 

1. Create a directory in your local computer 
2. Clone this Github repository
3. Run `docker-compose up --build`
4. Open a Browser and go to `http://localhost:8080`

